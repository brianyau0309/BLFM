swipe:
  3:
    left:
      command: 'xdotool key super+shift+Tab' # Next active tag
    right:
      command: 'xdotool key super+Tab' # Previous active tag
    up:
      command: 'xdotool key super+shift+space' # Switch layout to last layout
    down:
      command: 'xdotool key super+shift+m' # Switch layout to mono
  4:
    left:
      command: 'move-to-tag' # Move tag
    right:
      command: 'xdotool key super+q' # Close window
      threshold: 0.5
      interval: 1
    up:
      command: 'xdotool key super+shift+b' # Toggle bar
    down:
      command: 'xdotool key super+space' # Toogle floating

pinch:
  2:
    in:
      command: 'xdotool key ctrl+plus' # Zoom in
    out:
      command: 'xdotool key ctrl+minus' # Zoom out

rotate:
  3:
    clockwise:
      command: 'xdotool key XF86AudioRaiseVolume' # Raise Volume
    counterclockwise:
      command: 'xdotool key XF86AudioLowerVolume' # Lower Volume
  4:
    clockwise:
      command: 'xdotool key XF86MonBrightnessUp' # Increase Brightness
    counterclockwise:
      command: 'xdotool key XF86MonBrightnessDown' # Decrease Brightness

threshold:
  swipe: 0.2
  pinch: 0.5
  rotate: 0.3

plugin:
  inputs:
    libinput_command_input:
      enable-tap: true
      verbose: true
